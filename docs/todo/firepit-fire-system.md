# Firepit + Fire System

Firepit buildable on floor/dirt. Has `fuel` and `lit` state, consumes fuel per tick.

## Notes

- Jobs: Firekeeper (refuel), Cook (requires lit fire)
- Entropy: fires go out without maintenance
