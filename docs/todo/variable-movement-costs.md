# Variable Movement Costs

Road (cheap), rubble/mud (expensive) terrain costs.

## Notes

- Disables JPS/JPS+ (those algorithms assume uniform cost)
- Need to fall back to standard A* when variable costs are in use
